### Main Program
### For deployment

import rcdrive as rc;
import rcdrive2 as rc2;

# rc.drive(detectLanes=True, resolution=(80,64), flip=False);
# rc.stop();

rc2.drive(detectLanes=True, resolution=(80,64), flip=False);
rc2.stop();
